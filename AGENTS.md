# 🤖 AI Agent Engineering Guidelines

1. **Accessibility (a11y):** All buttons and interactive elements must have accessible labels and keyboard focus outlines.
2. **Performance Budget:** Do not add heavy external script tags without `defer` or `async`. Keep total asset bundle below 2MB.
